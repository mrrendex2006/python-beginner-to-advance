# 🐼 Pandas Practice (Python Libraries)

This file contains my hands-on practice with the **Pandas** library, focusing on data manipulation, analysis, and handling real-world datasets using Python.

---

## 🚀 Topics Covered

### 📌 1. Series & DataFrame Basics

* Creating `Series` with custom index
* Creating `DataFrame` from lists and dictionaries
* Understanding rows, columns, and structure

---

### 📂 2. Working with CSV Files

* Reading CSV files using `pd.read_csv()`
* Exploring dataset using:

  * `.head()`
  * `.tail()`
  * `.sample()`

---

### 📊 3. Data Inspection

* `.columns` → Column names
* `.shape` → Size of dataset
* `.size` → Total elements
* `.info()` → Data types and memory
* `.describe()` → Statistical summary

---

### 🔍 4. Data Access & Indexing

* Label-based indexing → `.loc`, `.at`
* Integer-based indexing → `.iloc`, `.iat`
* Filtering data using conditions

---

### 🔎 5. Querying Data

* Boolean filtering
* Using `.query()` for cleaner expressions

---

### ✏️ 6. Updating Data

* Modifying
